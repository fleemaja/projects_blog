import project from './project-schema';
import page from './page-schema';
import youtube from './youtube-schema';
import category from './category-schema';

const schemas = [project, page, youtube, category];

export default schemas;